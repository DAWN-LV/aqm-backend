export enum SensorStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  RESTARTING = 'RESTARTING',
}

export enum SensorType {
  CO2 = 'CO2',
  GAS = 'GAS',
  MOTION = 'MOTION',
  TEMPERATURE = 'TEMPERATURE',
  PRESSURE = 'PRESSURE',
  SMOKE = 'SMOKE',
  TEST = 'TEST'
}
